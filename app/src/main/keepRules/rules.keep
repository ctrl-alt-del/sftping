# R8 / ProGuard keep rules

# JSch (mwiede fork) — uses reflection for cipher negotiation
-keep class com.jcraft.jsch.** { *; }
-dontwarn com.jcraft.jsch.**

# Room entities must not be obfuscated
-keep class com.example.sftping.data.transfer.TransferTask { *; }
